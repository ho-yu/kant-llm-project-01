# Step 06

<일반적인 이커머스 고객 문의와 셀러 업무 질문에 적절하게 답변할 수 있는 로컬 LLM을 비교·평가하여 가장 적합한 모델을 선정한다.>

1. 실험 설계
--- 로컬 모델 6개(Model A~F) × 질문 10개 × 각 2회 = 본 실험 120회 ---
--- (과제 필수 최소치는 로컬 2개 기준 40회. 6개는 확장 실험) ---
--- Model A·F는 CLI 사전 확인에서 문제가 보였으나 본 실험 결과로 판정한다 ---
--- 모델당 워밍업 1회는 본 실험과 별도로 기록한다. ---


2. 실행 기록


3. 품질 채점


4. 결과 해석



## 실행 기록 삭제 이력

원본 기록은 append 전용이며 삭제하지 않는 것이 원칙이다.
아래는 그 예외이며, 무엇을 왜 지웠는지 남긴다.

**2026-09-15 — Model F 기록 2건 삭제**

| run_id | phase | timestamp | status |
|---|---|---|---|
| `F_warmup` | warmup | 2026-09-15T12:43:43+09:00 | error / ResponseError |
| `F_Q01_r1` | main | 2026-09-15T12:44:46+09:00 | error / ResponseError |

- 대상 모델 태그: `hf.co/RichardErkhov/x2bee_-_POLAR-14B-v0.5-gguf:Q4_K_M`
- 오류 내용 (2건 동일):

  ```
  llama-server chat error: map[code:500 message:The model produced output that
  does not match the expected peg-native format type:server_error] (status code: 500)
  ```

- **삭제 사유**: 해당 GGUF 변환본이 응답 포맷 오류로 호출 자체가 불가능해,
  다른 변환본을 새로 내려받아 같은 라벨(F)로 재실험하기로 했다.
  `run_id` 가 같으면 중복으로 건너뛰므로 이전 기록을 제거했다.
- 삭제 시점 이후의 F 기록은 **교체된 모델의 결과**이며, 위 오류는 이 표로만 남는다.
- CLI 사전 확인에서 관측한 같은 증상은
  [eval-results.md](../eval-results.md) 'STEP 4 CLI 스모크 테스트에서 관찰된 문제' 절에도 기록되어 있다.

**2026-09-15 14:42 — Model F 기록 2건 추가 삭제**

| run_id | phase | timestamp | status |
|---|---|---|---|
| `F_warmup` | warmup | 2026-09-15T14:42:22+09:00 | error / ResponseError |
| `F_Q01_r1` | main | 2026-09-15T14:42:22+09:00 | error / ResponseError |

- 대상 모델 태그: `hf.co/RichardErkhov/x2bee_-_POLAR-14B-v0.5-gguf:Q4_K_M` (위와 동일)
- 오류 내용: 위와 동일한 `peg-native format` 500 오류
- **삭제 사유**: 첫 삭제 뒤 모델 태그를 바꾸기 전에 같은 변환본으로 한 번 더
  실행되어 같은 오류가 재기록되었다. 교체 모델로 재실험하기 위해 제거했다.
- **의미**: 이 변환본은 서로 다른 시각에 **두 차례(12:43, 14:42) 모두 동일하게 실패**했다.
  일회성 오류가 아님을 보여주는 근거다.




---

## 산출물 정리

> 연결 산출물: [deliverables.md](../deliverables.md)
> - `3. Model Test / Benchmark 결과` ← **이 스텝이 원본**
> - 요구사항 충족도 평가표 **#5. 로컬 비교 실험**, **#6. 품질 평가/해석**

### 원본 기록에 반드시 들어갈 필드

> JSON / CSV / JSONL 등 **다시 읽을 수 있는 형식**으로 저장한다.
> 비교표의 모든 수치는 이 기록에서 나와야 하고, 역추적이 가능해야 한다.

| 필드 | 설명 |
|---|---|
| `run_id` | 실행 ID |
| `question_id` | Q01~Q10 |
| `model` | 모델 태그 (양자화 포함 전체 식별값) |
| `repeat` | 반복 회차 (1 / 2) |
| `is_warmup` | 워밍업 여부 — true면 본 실험 집계에서 제외 |
| `prompt` | 실제 입력 전문 |
| `response` | 원본 응답 전문 |
| `status` | 성공 / 오류 |
| `error` | 오류 내용 (실패 시) |
| `latency_total_s` | 전체 응답 시간 |
| `load_duration_s` | 모델 로딩 시간 |
| `output_tokens` | 출력 토큰 수 |
| `tokens_per_s` | 생성 속도 |
| `vram_mb` | VRAM 사용량 |
| `options` | temperature / num_predict / num_ctx 등 실행 설정 |
| `timestamp` | 실행 시각 |

### 집계 시 반드시 표시할 것 (평가표 #5·#6)

- [ ] 모델별 **호출 성공 수 / 전체 시도 수** (예: 19/20)
- [ ] 지표별 **집계 응답 수(n)** — 평균만 적지 말 것
- [ ] 측정 불가 항목이 있으면 **사유** 기재
- [ ] 워밍업 / 재시도 / 추가 실험은 본 실험 120회와 **분리 집계**
- [ ] **실패**(호출 오류)와 **측정 누락**(호출은 성공했으나 값 없음)을 구분

### 모델별 집계표 (작성용)

| 지표 | Model A (법률) | Model B (의료·바이오) | Model C (금융) | Model D (코딩) | Model E (수학) | Model F (이커머스) |
|---|---|---|---|---|---|---|
| 호출 성공 / 전체 시도 | | | | | | |
| 평균 전체 응답 시간 | | | | | | |
| 평균 로딩 시간 | | | | | | |
| 평균 출력 토큰 수 | | | | | | |
| 평균 생성 속도 | | | | | | |
| VRAM 사용량 | | | | | | |
| 평균 품질 점수 | | | | | | |

> 각 수치 옆에 **집계 응답 수(n)** 를 함께 적는다. 모델당 시도는 20회(10문제 × 2회).
> Model A·F의 CLI 사전 관찰 내용은 [eval-results.md](../eval-results.md) 'STEP 4 CLI 스모크 테스트에서 관찰된 문제' 절 참조.

### 품질 평가 (평가표 #6)

- [ ] STEP 5 채점 기준을 모든 응답에 동일하게 적용
- [ ] 점수의 근거가 되는 **응답의 특정 부분**을 인용
- [ ] 대표 성공 사례 선정
- [ ] 대표 실패 사례 선정 (STEP 8 보고서에서 재사용)
- [ ] 개인 수행이므로 **재검토**(2차 확인) 수행 및 기록

### 제출 시 확인

- [ ] 비교표의 모든 수치를 원본 기록에서 역추적 가능
- [ ] AI가 생성한 예시 결과가 섞이지 않음 (실제 실행 결과만 제출)
- [ ] 원본 기록 파일 위치가 README에 안내되어 있음

---

<!-- evalkit:auto:start — 아래는 자동 생성 구간입니다. 직접 고치지 마세요. -->

## STEP 06 — 성능 측정

| 지표 | Model A (법률) | Model B (의료·바이오) | Model C (금융) | Model D (코딩) | Model E (수학) | Model F (이커머스) |
|---|---|---|---|---|---|---|
| 호출 성공 / 전체 시도 | 20 / 20 | 20 / 20 | 20 / 20 | 20 / 20 | 20 / 20 | 20 / 20 |
| 평균 전체 응답 시간 | 12.45s (n=20) | 7.84s (n=20) | 2.89s (n=20) | 4.05s (n=20) | 4.74s (n=20) | 3.33s (n=20) |
| 평균 로딩 시간 | 0.23s (n=20) | 0.00s (n=6) | 0.21s (n=20) | 0.21s (n=20) | 0.21s (n=20) | 0.20s (n=20) |
| 평균 출력 토큰 수 | 768.0 (n=20) | 768.0 (n=6) | 158.4 (n=20) | 252.0 (n=20) | 288.0 (n=20) | 187.1 (n=20) |
| 평균 입력 토큰 수 | 53.7 (n=20) | 38.8 (n=6) | 52.7 (n=20) | 44.4 (n=20) | 44.4 (n=20) | 44.4 (n=20) |
| 평균 생성 속도 | 63.42 t/s (n=20) | 59.23 t/s (n=6) | 61.13 t/s (n=20) | 66.97 t/s (n=20) | 65.33 t/s (n=20) | 61.58 t/s (n=20) |
| VRAM (관측 시점) | 5027.5 MiB (n=20) | 5027.5 MiB (n=20) | 5027.5 MiB (n=20) | 4528.1 MiB (n=20) | 4528.1 MiB (n=20) | 4528.1 MiB (n=20) |

> 집계 범위: 본 실험만 (워밍업·재시도 제외). 파일 전체 126건 중 120건.
> `n` 은 지표마다 다르다. 측정값을 얻지 못한 회차는 그 지표의 평균과 `n` 에서 제외된다.
> VRAM 은 응답 직후 관측 시점의 값이며 최대 VRAM 이 아니다.
> CPU/GPU 적재 상태는 GPU 이용률이 아니다.

### 실패 / 측정 누락

| 회차 | 구분 | 사유 |
|---|---|---|
| B_Q01_r2, B_Q02_r1, B_Q03_r1 외 11건 | 측정 누락 | load_duration_sec — 응답에 load_duration 없음 |
| B_Q01_r2, B_Q02_r1, B_Q03_r1 외 11건 | 측정 누락 | eval_count — 응답에 eval_count 없음 |
| B_Q01_r2, B_Q02_r1, B_Q03_r1 외 11건 | 측정 누락 | eval_duration_sec — 응답에 eval_duration 없음 |
| B_Q01_r2, B_Q02_r1, B_Q03_r1 외 11건 | 측정 누락 | tokens_per_sec — eval_count 또는 eval_duration 없음 |
| B_Q01_r2, B_Q02_r1, B_Q03_r1 외 11건 | 측정 누락 | prompt_eval_count — 응답에 prompt_eval_count 없음 |

<!-- evalkit:auto:end -->
